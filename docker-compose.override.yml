# docker-compose.override.yml — loaded automatically by Docker Compose.
# This file is for LOCAL DEVELOPMENT ONLY and should NOT be used in production.
#
# Features enabled here:
# - Debug ports (5670-5678) for attaching debuggers to Python services
# - Bind mounts for admin_web dev/hot-reload mode
#
# To run in production mode instead, set in .env:
#   BUILD_TARGET=runner
#   DEBUG=false
# And ensure this file is NOT present (use -f docker-compose.yml explicitly)

services:
  telegram_bot:
    ports:
      - "5670:5671"

  auth_service:
    ports:
      - "5672:5671"

  checklists_service:
    ports:
      - "5673:5671"

  knowledge_service:
    ports:
      - "5674:5671"

  notification_service:
    ports:
      - "5675:5671"

  feedback_service:
    ports:
      - "5676:5671"

  escalation_service:
    ports:
      - "5677:5671"

  meeting_service:
    ports:
      - "5678:5671"

  admin_web:
    volumes:
      - ./admin_web:/app
