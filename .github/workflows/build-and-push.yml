name: Build and push images

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "LICENSE"
  workflow_dispatch:
    inputs:
      tag:
        description: "Extra tag to apply (optional)"
        required: false
      force_rebuild:
        description: "Force rebuild all services (ignore change detection)"
        type: boolean
        required: false
        default: true

permissions:
  contents: read

env:
  REGISTRY: docker.io
  # Image names: docker.io/<DOCKERHUB_USERNAME>/mentor-bot-<service>
  # Must match docker-compose.prod.yml IMAGE_PREFIX default (mentor-bot)
  IMAGE_PREFIX: mentor-bot
  PLATFORMS: linux/amd64

jobs:
  # ── Detect changed services ───────────────────────────────────────────────────
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      auth_service: ${{ steps.changes.outputs.auth_service }}
      checklists_service: ${{ steps.changes.outputs.checklists_service }}
      knowledge_service: ${{ steps.changes.outputs.knowledge_service }}
      notification_service: ${{ steps.changes.outputs.notification_service }}
      escalation_service: ${{ steps.changes.outputs.escalation_service }}
      feedback_service: ${{ steps.changes.outputs.feedback_service }}
      meeting_service: ${{ steps.changes.outputs.meeting_service }}
      telegram_bot: ${{ steps.changes.outputs.telegram_bot }}
      admin_web: ${{ steps.changes.outputs.admin_web }}
      dockerfile: ${{ steps.changes.outputs.dockerfile }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Detect changes
        id: changes
        uses: dorny/paths-filter@v4
        with:
          filters: |
            auth_service:
              - 'auth_service/**'
            checklists_service:
              - 'checklists_service/**'
            knowledge_service:
              - 'knowledge_service/**'
            notification_service:
              - 'notification_service/**'
            escalation_service:
              - 'escalation_service/**'
            feedback_service:
              - 'feedback_service/**'
            meeting_service:
              - 'meeting_service/**'
            telegram_bot:
              - 'telegram_bot/**'
            admin_web:
              - 'admin_web/**'
            dockerfile:
              - 'Dockerfile'
              - 'docker-entrypoint.sh'

  # ── Test gate: must pass before image is built ───────────────────────────────
  test-python-auth:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.auth_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "auth_service/uv.lock"
      - name: Install dependencies
        run: make deps-auth
      - name: Lint (ruff)
        run: make lint-auth || true
      - name: Type check (mypy)
        run: make mypy-auth || true
      - name: Run tests
        run: make test-auth

  test-python-checklists:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.checklists_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "checklists_service/uv.lock"
      - name: Install dependencies
        run: make deps-checklists
      - name: Lint (ruff)
        run: make lint-checklists || true
      - name: Type check (mypy)
        run: make mypy-checklists || true
      - name: Run tests
        run: make test-checklists

  test-python-knowledge:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.knowledge_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "knowledge_service/uv.lock"
      - name: Install dependencies
        run: make deps-knowledge
      - name: Lint (ruff)
        run: make lint-knowledge || true
      - name: Type check (mypy)
        run: make mypy-knowledge || true
      - name: Run tests
        run: make test-knowledge

  test-python-notification:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.notification_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "notification_service/uv.lock"
      - name: Install dependencies
        run: make deps-notification
      - name: Lint (ruff)
        run: make lint-notification || true
      - name: Type check (mypy)
        run: make mypy-notification || true
      - name: Run tests
        run: make test-notification

  test-python-escalation:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.escalation_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "escalation_service/uv.lock"
      - name: Install dependencies
        run: make deps-escalation
      - name: Lint (ruff)
        run: make lint-escalation || true
      - name: Type check (mypy)
        run: make mypy-escalation || true
      - name: Run tests
        run: make test-escalation

  test-python-feedback:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.feedback_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "feedback_service/uv.lock"
      - name: Install dependencies
        run: make deps-feedback
      - name: Lint (ruff)
        run: make lint-feedback || true
      - name: Type check (mypy)
        run: make mypy-feedback || true
      - name: Run tests
        run: make test-feedback

  test-python-meeting:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.meeting_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "meeting_service/uv.lock"
      - name: Install dependencies
        run: make deps-meeting
      - name: Lint (ruff)
        run: make lint-meeting || true
      - name: Type check (mypy)
        run: make mypy-meeting || true
      - name: Run tests
        run: make test-meeting

  test-python-telegram:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.telegram_bot == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true
          cache-dependency-glob: "telegram_bot/uv.lock"
      - name: Install dependencies
        run: make deps-telegram
      - name: Lint (ruff)
        run: make lint-telegram || true
      - name: Type check (mypy)
        run: make mypy-telegram || true
      - name: Run tests
        run: make test-telegram

  test-admin-web:
    needs: detect-changes
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.admin_web == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: make deps-admin

      - name: Lint
        run: make lint-admin || true

      - name: Run tests
        run: make test-admin

  # ── Build + push Python services ────────────────────────────────────────────
  build-auth:
    needs: [detect-changes, test-python-auth]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.auth_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-auth_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=auth_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=auth_service
          cache-to: type=gha,mode=max,scope=auth_service

  build-checklists:
    needs: [detect-changes, test-python-checklists]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.checklists_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-checklists_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=checklists_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=checklists_service
          cache-to: type=gha,mode=max,scope=checklists_service

  build-knowledge:
    needs: [detect-changes, test-python-knowledge]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.knowledge_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-knowledge_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=knowledge_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=knowledge_service
          cache-to: type=gha,mode=max,scope=knowledge_service

  build-notification:
    needs: [detect-changes, test-python-notification]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.notification_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-notification_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=notification_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=notification_service
          cache-to: type=gha,mode=max,scope=notification_service

  build-escalation:
    needs: [detect-changes, test-python-escalation]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.escalation_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-escalation_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=escalation_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=escalation_service
          cache-to: type=gha,mode=max,scope=escalation_service

  build-feedback:
    needs: [detect-changes, test-python-feedback]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.feedback_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-feedback_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=feedback_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=feedback_service
          cache-to: type=gha,mode=max,scope=feedback_service

  build-meeting:
    needs: [detect-changes, test-python-meeting]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.meeting_service == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-meeting_service
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=meeting_service
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=meeting_service
          cache-to: type=gha,mode=max,scope=meeting_service

  build-telegram:
    needs: [detect-changes, test-python-telegram]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.telegram_bot == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-telegram_bot
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          target: prod
          platforms: ${{ env.PLATFORMS }}
          build-args: |
            SERVICE_NAME=telegram_bot
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=telegram_bot
          cache-to: type=gha,mode=max,scope=telegram_bot

  # ── Build + push admin web ───────────────────────────────────────────────────
  build-admin-web:
    needs: [detect-changes, test-admin-web]
    runs-on: ubuntu-latest
    if: |
      needs.detect-changes.outputs.admin_web == 'true' ||
      needs.detect-changes.outputs.dockerfile == 'true' ||
      github.event.inputs.force_rebuild == 'true'
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_PREFIX }}-admin_web
          tags: |
            type=sha,format=short
            type=ref,event=branch
            type=ref,event=tag
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./admin_web
          file: ./admin_web/Dockerfile
          target: runner
          platforms: ${{ env.PLATFORMS }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          cache-from: type=gha,scope=admin_web
          cache-to: type=gha,mode=max,scope=admin_web
